import PageStub from '../components/ui/PageStub.jsx'

export default function SpendDashboard() {
  return (
    <PageStub
      title="Spend"
      sub="LLM cost across vendors, models, agents, and fleets"
      icon="$"
      note="Vendor / model / agent spend breakdown with SVG charts lands in Session 8."
    />
  )
}
