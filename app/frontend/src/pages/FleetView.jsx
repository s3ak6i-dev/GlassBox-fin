import PageStub from '../components/ui/PageStub.jsx'

export default function FleetView() {
  return (
    <PageStub
      title="Fleet View"
      sub="Topology graph of agents, vendors, and the calls between them"
      icon="⬡"
      note="The D3 force-directed compliance topology graph lands in Session 7 — agents and vendors as nodes, calls as edges, violations as animated red paths."
    />
  )
}
