import PageStub from '../components/ui/PageStub.jsx'

export default function Settings() {
  return (
    <PageStub
      title="Settings"
      sub="Organization, team, API keys, vendors, and notifications"
      icon="⚙"
      note="Settings tabs (team, instrumentation keys, vendor config, approver routing) land in Session 8."
    />
  )
}
